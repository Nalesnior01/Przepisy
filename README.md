# Cookbook – English version

English recipe pages prepared for importing into Samsung Food via URL.

Each recipe is a standalone HTML page containing Schema.org Recipe JSON-LD.
The original Carbonara entry was not included because the source document contained only the recipe title and no ingredients or instructions.
